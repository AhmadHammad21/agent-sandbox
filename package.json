{
  "name": "agent-sandbox",
  "version": "0.1.0",
  "description": "Isolated, stateful AI agents for every tenant — open-source multi-tenant agent infrastructure.",
  "license": "AGPL-3.0",
  "type": "module",
  "module": "src/api/server.ts",
  "scripts": {
    "dev": "bun --watch src/api/server.ts",
    "start": "bun src/api/server.ts",
    "migrate": "bun src/db/migrate.ts",
    "provision": "bun src/api/tenants/provision.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "example": "bun src/examples/basic-session.ts",
    "demo:two-tenants": "bun tests/two-tenants-demo.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@daytonaio/sdk": "^0.187.0",
    "express": "^4.21.2",
    "openai": "^6.42.0",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@types/express": "^5.0.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
